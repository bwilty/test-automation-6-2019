###
# This file just holds a class with configuration constants (just one for now).
# This helps to prevent hardcoding context-specific values in our code if,
# for example, we wanted to switch between testing on a mock server and on
# a production server.
###

class Configuration
   
    DOMAIN = "http://localhost"

end